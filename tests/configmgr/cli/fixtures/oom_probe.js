// oom_probe.js -- the smallest script that exercises configmgr startup and
// the -script evaluation path, for test_oom_no_crash.sh.
import * as std from "std";
console.log("oom_probe ran");
std.exit(0);
